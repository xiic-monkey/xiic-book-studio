# 2026-08-09

## 项目分析
- 完整梳理了 xiic-book-studio：Tauri 2 + React 19 桌面端 AI 小说创作工作台
- 确认技术栈、后端分层架构、领域模块、当前 git 状态（架构迁移途中 + 包管理混用）
- 结论已写入 MEMORY.md

## 设定资料设计讨论（00:04）
- 用户指出：设定资料的"当前确认版本"是 md 硬拆卡片（`parseKnowledgeSections` 按 `##` 拆分 + `cleanKnowledgeText` 正则清洗），很奇怪；主张设定资料本就该是一张张结构化卡片（title/type/content），Agent 增删改应走工具调用。
- 小柚判断：认同。病灶 = 设定资料主路径是 setting 阶段 markdown artifact 硬拆，与另一套 `KnowledgeCard` 并存，category 由前端硬编码映射，脆弱。
- 改造方向（待用户确认动手）：① 设定资料统一为 `KnowledgeCard` 实体；② Agent 工具补齐 create/update/delete 三件套，均走 `AdoptionProposal` → 人工采纳；③ 前端移除 md 硬拆渲染，只渲 `KnowledgeCard`；④ 处理旧 setting markdown 迁移；⑤ 排查 `libraryArtifact` 其他引用（审校/上下文来源）。

# 2026-08-11

## 打包并覆盖本地启动台安装
- 需求：把前一天整理好的右侧 agent 侧栏 UI 改动打包，覆盖 /Applications 中已安装的 app。
- 构建：`npm run tauri:build`（= `bundle:prepare` 资源校验 + `tauri build`）。资源已缓存（bge 模型 + web-search-mcp 运行时走缓存秒过）；前端 vite 重打包、Rust 增量编译 53s；产出 `src-tauri/target/release/bundle/macos/Xiic Book Studio.app` 及同名 .dmg。
- 安装：`rm -rf` 旧 `/Applications/Xiic Book Studio.app` + `ditto` 复制新包（无需 sudo——`/Applications` 为 `root:admin` 且组可写，当前用户 xiic 属 admin；旧包也归 xiic 所有）；`xattr -dr com.apple.quarantine` 去掉隔离属性防 Gatekeeper 拦截。
- 校验：app 正常拉起（PID 57388，无崩溃）；构建顺序 vite→cargo embed→bundle，dist 含新文案，二进制 mtime 新鲜。

## 重要认知：Tauri 2 嵌入资源验证方式
- Tauri 2 把 `frontendDist` 在编译期嵌入二进制（Resources 里只有 web-search 的 playwright 文件，没有 app 自己的 JS），且为压缩/编码态。
- 因此 `grep "本次运行" bundle/Contents` 找不到明文是**正常现象**，不是改动缺失。
- 正确验证：`grep` 预打包的 `dist/assets/*.js` 确认含改动；再确认构建顺序 vite→cargo、二进制 mtime 新鲜、app 能启动即可。
- `grep ... | head && echo FOUND` 是假阳性陷阱——head 退出码 0 会触发 echo，应用 `grep -q` 或 `grep ... | head -1` 配合正确判读。

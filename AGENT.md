# UI 文案规范

## 界面只说当下要做的事

- UI 文案只保留标题、字段名、按钮、短状态、空状态和必要的错误信息。
- 不把工作流说明、产品规则、实现原理、数据来源、未来规划或操作教程写进界面段落。
- 用“状态 + 动作”表达限制，例如“正式资料 · 只读”，配合“去工作台”按钮；不要解释为什么、下一步完整流程是什么。
- 空状态尽量使用短句，动作由相邻按钮承担；不要用“可以……或……”列举操作路径。
- `placeholder` 是字段提示，不是教程；使用字段名或极短示例，不写完整写作方法。
- `title`/`data-tip` 只用于图标等确实需要解释的控件，保持一句短语；不得借 tooltip 塞入产品说明。
- 动态业务内容（故事正文、资料卡内容、审校报告、模型错误详情）按原样展示，不把它们当作静态 UI 文案改写。

## 说明放在哪里

- 产品规则和使用流程写入 `docs/`。
- 实现原因和边界写在代码附近的简短注释中。
- 新增或修改 UI 时，优先删除重复说明；导航、状态和按钮已经表达的信息不再重复解释。

提交前检查静态文案：检索 JSX/TSX 中的长句、`settings-hint`、`read-only-note`、`data-tip` 和说明性 `placeholder`，确认它们没有把界面变成操作手册。
